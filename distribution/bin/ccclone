#! /usr/bin/env bash

# ccclone is git clone for codecommit - a thin wrapper around 'cch repo clone'
if [ -z "$1" ]
then
    echo "ccclone: No repository name supplied - use cclist to get a list of repositories"
    exit 1
fi

exec cch repo clone "$1"
