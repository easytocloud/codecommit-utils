#!/usr/bin/env bash

# ccinit is a thin wrapper around 'cch repo create --readme'

# Check if repository name is passed as a parameter
if [ -z "$1" ]
then
    echo "No repository name supplied"
    exit 1
fi

exec cch repo create "$1" --readme
